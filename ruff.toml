target-version = "py310"

[lint]
# TRY002/BLE001 match existing API error-handling patterns (raise Exception, broad except).
# UP017: datetime.UTC requires Python 3.11+; Docker runs 3.10.
ignore = ["TRY002", "BLE001", "UP017"]
